# Configuration

The installer uses `~/.claude`, `~/.codex`, and `~/.local/state` by default. Override them with `CLAUDE_HOME`, `CODEX_HOME`, and `XDG_STATE_HOME`.

Installed command and skill entry points record the absolute path to this checkout's `workflow.md`. Re-run the installer after moving the checkout.
