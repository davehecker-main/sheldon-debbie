# Permissions

The installer writes only beneath the configured Claude, Codex, and state homes. Runtime definitions are read-only advisors. They may inspect repositories and public web sources but must not edit files, post comments, change tracker state, or merge work.
