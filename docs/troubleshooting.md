# Troubleshooting

## The tool is not discovered

Run `./scripts/install.sh`, then restart or reload clients that cache definitions.

## The workflow path is stale

Re-run the installer after moving the checkout.

## A previous definition is needed

Restore it from `~/.local/state/personal-tools/backups/sheldon-debbie/<timestamp>/` or the configured state home.
